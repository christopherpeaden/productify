<%= form_for(@list) do |f| %>
  <%= f.label "Title" %>
  <%= f.text_field :title %>

  <%= f.label "Description" %>
  <%= f.text_field :description%>

  <%= f.hidden_field :category_id, :value => @category.id %>

  <%= f.submit "Submit" %>
<% end %>
