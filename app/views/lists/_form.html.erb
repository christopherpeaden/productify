<%= form_for([@category, @list]) do |f| %>
  <%= f.label "Title" %>
  <%= f.text_field :title %>

  <%= f.label "Description" %>
  <%= f.text_field :description%>

  <%= f.submit "Submit" %>
<% end %>
